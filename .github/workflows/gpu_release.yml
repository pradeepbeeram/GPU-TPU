name: GPU POC Release

on:
  workflow_dispatch:

env:
  PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}
  ZONE: ${{ vars.GKE_ZONE }}
  CLUSTER_NAME: ${{ vars.GPU_CLUSTER_NAME }}
  GPU_POOL_NAME: ${{ vars.GPU_POOL_NAME }}

jobs:
  gpu-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
